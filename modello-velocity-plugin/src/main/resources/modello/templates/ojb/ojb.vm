    <!-- OJB repository-user.xml generated by Modello -->
#foreach ( $class in $model.allClasses )
#if ( $class.getMetadata( $id ).isStorable() )
  <class-descriptor class="${package}.${class.name}" table="$class.name">
#foreach ( $field in $class.getFields( $version ) )
    <field-descriptor name="$field.name" column="$field.name"/>
#end
  </class-descriptor>
#end
#end